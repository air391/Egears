[project]
name = "gears-viz"
version = "0.1.0"
description = "Analysis tools for GEARS ROOT output (uproot backend)"
requires-python = ">=3.13"
dependencies = [
    "uproot>=5.3",
    "matplotlib>=3.8",
    "numpy>=1.26",
]
